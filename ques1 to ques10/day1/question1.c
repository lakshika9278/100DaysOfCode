// Question: Write a program to add two numbers.
#include <stdio.h>

int main() {
    int a, b;
    // taking input
    scanf("%d %d", &a, &b);
    // printing sum
    printf("%d", a + b);
    return 0;
}
